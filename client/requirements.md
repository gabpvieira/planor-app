## Packages
date-fns | Date manipulation and formatting
react-day-picker | Calendar component for Agenda
recharts | Charts for Habits, Goals, Finance
framer-motion | Smooth animations and transitions
clsx | Utility for constructing className strings conditionally
tailwind-merge | Utility for merging Tailwind classes safely

## Notes
Authentication uses Replit Auth (backend integrated).
Client checks `/api/auth/user` via `useAuth` hook.
Redirect unauthenticated users to Landing Page.
Dashboard uses a Sidebar layout.
Uses Lucide React for icons.
Stunning minimalist design with Inter font.
